import styled from 'styled-components';

export const LoginProblemButton = styled.button`
  font-size: ${({ theme }) => theme.fontSize.m};
  color: ${({ theme }) => theme.colors.black};
  border: none;
  background: none;
`;
