import styled from 'styled-components';

export const LabelAboveInput = styled.label`
  color: ${({ theme }) => theme.colors.labelBlue};
  font-size: ${({ theme }) => theme.fontSize.l};
  margin: 0 0 0.25em 0;
`;
